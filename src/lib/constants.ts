// Re-export from config so existing imports keep working.
// Kalau ingin rebranding, edit src/config/app.config.ts — bukan file ini.
export {
  CHART_COLORS as COLORS,
  APPS_STORAGE_KEY,
  SELECTED_APP_STORAGE_KEY,
} from '../config/app.config';
